import 'package:flutter/material.dart';

class Paychart extends StatelessWidget {
  const Paychart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(child: ,);
  }
}
